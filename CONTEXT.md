# TvLink

TvLink 是一个 Tavily API Key 池：用一批独立的 Tavily Key 为客户端提供统一的 REST 与 MCP 入口，同时控制每把 Key 的使用节奏。

## Language

**Tavily Key（Key）**:
一把独立计费的 Tavily 凭据；当前部署中每把 Key 属于一个独立账号，额度与限流互不共享。
_Avoid_: API key（易与客户端使用的 `tvlink_api_key` 混淆）

**组（Group）**:
若干 Key 组成的较长且稳定的使用窗口，同时承担额度轮换与使用节奏控制。存在理由：在固定出站 IP 下避免全池 Key 短时间内同时参与分配、避免整池流量呈现为同源负载均衡。分组不改变出站 IP，控制的是节奏而不是身份。
_Avoid_: 池（Pool 指整个 Key 集合）

**轮次（Round）**:
一个组从被激活到额度用尽或被延后的使用周期；重建分组即开始新一轮。

**活动组（Active Group）**:
当前轮次中唯一承担请求的组。

**终态（Terminal）**:
组在本轮不再参与选路的状态：额度用尽、本轮延后、剩余预算装不下当前请求而封闭。「延后」只表示暂不可用，不表示额度已消耗。
_Avoid_: 关闭（与「主动封闭」混用）

**用量快照（Usage Snapshot）**:
Tavily `/usage` 返回的权威用量，是真实用量的唯一来源；该端点有独立限流（按 Key，10 次/10 分钟）。
_Avoid_: 实时用量

**真实用量 / 预估用量**:
真实用量来自用量快照；预估用量是 TvLink 对在途请求的本地预扣，用于两次快照之间估计剩余额度。

**刷新槽（Refresh Slot）**:
一个组在刷新周期内专属的时间段：组内 Key 在本组槽内刷新用量快照，组间错开。
_Avoid_: 定时器、批次

**刷新节流（Refresh Throttle）**:
同一把 Key 两次 `/usage` 之间的最小间隔约束，所有刷新入口共用；被 429 限流时按 Retry-After（缺省 300 秒）延长。
_Avoid_: 冷却（专指 Key 因 429 进入的不可用状态）

**外部调用源**:
不经过 TvLink、直接使用同一批 Key 的调用者；其消耗只能通过用量快照观察到。

**冷却（Cooling）/ 探测（Probing）/ 耗尽（Exhausted）**:
Key 的三种不可用状态：429 后等待 Retry-After、冷却结束后的单次试探、Tavily 报告额度已用尽。
